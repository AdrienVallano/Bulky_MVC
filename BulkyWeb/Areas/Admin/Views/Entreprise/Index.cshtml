@model List<Entreprise>

<partial name="_Notification"/>
    <div class="container">
        <div class="row pt-4 pb-3">
            <div class="col-6">
            <h2 class="">Liste des entreprises</h2>
            </div>
                
            <div class="col-6 text-end">
            <a asp-area="Admin" asp-controller="Entreprise" asp-action="Upsert" class="btn btn-primary"><i class="bi bi-plus-circle"></i> Ajout entreprise partenaire</a>
            </div>

        </div>

    <table id="entTable" class="table table-hover table-striped">
        <thead>
    <tr class="">
            <th class="bg-secondary">Nom</th>
            <th class="bg-secondary">Adresse</th>
            <th class ="bg-secondary">Ville</th>
            <th class="bg-secondary">Code</th>
            <th class ="bg-secondary">Telephone</th>
            <th class="bg-secondary"></th>
            </tr>
        </thead>
</table>
</div>

@section Scripts{
    <script src="/js/entreprise.js"></script>
}
